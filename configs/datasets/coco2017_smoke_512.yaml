# Larger real-data import for adapter scaling experiments.
# COCO has one row per image and multiple human captions in `captions`.
dataset_id: phiyodr/coco2017
split: train
output_root: data/coco2017_smoke_512
image_column: coco_url
caption_column: captions
id_column: image_id
clip_score_column: null
limit: 512
